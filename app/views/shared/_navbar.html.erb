
<nav class="navbar navbar-expand-lg bg-body-tertiary">
<div class="container-fluid">
<a class="navbar-brand" href="#" style="color: #ff0000; font-weight: bold;">ParamVista</a>

  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="#">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Link</a>
      </li>
      
    </ul>
  <% if user_signed_in? %>
    <%= current_user.email %> &nbsp;
    <div class="buttons">
      <%= link_to 'Log out', destroy_user_session_path, class: 'btn btn-danger', data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }  %>
     <%# <%= link_to 'Email Settings', edit_user_registration_path, class: 'btn btn-primary' %>
    </div>
  <% else %>
    <div class="buttons">
      <%= link_to 'Log in', new_user_session_path, class: 'btn btn-primary' %>
      <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-success' %>
    </div>
  <% end %>
  </div>
</div>
</nav>